//»ðÐÇÎÄ×Öµä
#include <algorithm> 
#include <cstdio>
#include <cstdlib>
#include <vector>
#include <queue>
#include <stack>
#include <set> 
#include <map>
#include <iostream>
#include <cstring>
#include <sstream>
#include <iterator>
using namespace std;
#define ll long long
#define inf 0x3f3f3f3f
#define mem(a,b) memset(a,b,sizeof(a))
#define closeio std::ios::sync_with_stdio(false)
const int MAX_N=1000;
int main(){
//	vector<string> v;
//	vector<char> res;
//	map<char,int> m;
//	string str;
//	getline(cin,str);
//	string ts="";
//	for(int i=0;i<str.length();i++){
//		if(str[i]!=' '){
//				ts+=str[i];
//				if(i==str.length()-1)	v.push_back(ts);
//		}
//		else{
//			v.push_back(ts);
//			ts="";
//		} 
//	}
//	for(int i=0;i<v.size();i++){
//		if(m.count(v[i][0])>0)	continue;
//		else{
//			m[v[i][0]]=1;
//			res.push_back(v[i][0]);
//		}	
//	}
	string s;
	vector<string> v;
	while(cin>>s) 
	return 0;
}
